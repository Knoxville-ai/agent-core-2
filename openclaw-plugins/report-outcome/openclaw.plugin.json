{
  "id": "knox-report-outcome",
  "name": "Knox Report Outcome",
  "description": "Stamp the platform conversation id onto the agent's report_outcome MCP call so the model never has to know or type it.",
  "activation": {
    "onStartup": true
  },
  "configSchema": {
    "type": "object",
    "additionalProperties": false
  }
}
